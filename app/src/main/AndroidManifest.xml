<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.bqt">

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.ViewPager_ListView" />
        <activity android:name=".activity.ListView_ViewPager" />
        <activity android:name=".activity.ScrollView_ListView" />
        <activity android:name=".activity.ScrollView_ListView_2" />
        <activity android:name=".activity.ScrollView_ListView_W" />
        <activity android:name=".activity.ScrollView_ListView_N" />
    </application>

</manifest>